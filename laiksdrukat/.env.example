# Database — replace with your MySQL credentials
DATABASE_URL="mysql://user:password@localhost:3306/laiksdrukat"

# Session secret — change this to a long random string in production
SESSION_SECRET="change-this-to-a-long-random-secret-string-in-production"

# Server
PORT=3000
HOST=0.0.0.0

# Admin (initial password is set in seed, change after first login)
ADMIN_USERNAME=admin
